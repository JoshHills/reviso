/* File encapsulates the bespoke demonstration functions for this page. */

function exampleFunction() {

    // Define a string to alert.
    var message = "Hello " + user.name + "!";
    
    // Display the alert.
    window.alert(message);

}